{% extends 'base.html.twig' %}
{% block title "Edition d'une marque" %}

{% block body %}
    <div class="row m-0">
        <div class="menu-admin d-lg-block col-lg-2 d-none">
            {{ include('layout/sidebar.html.twig') }}
        </div>
        <div class="container-admin col-lg-10 col-12">
            <div class="container mt-4">
                <h2>Edition de la marque n° <span class="text-primary">{{ brand.id }}</span></h2>

                {{ form_start(form) }}
                <div class="row">
                    <div class="offset-lg-3 col-lg-6 offset-md-1 col-md-10">{{ form_row(form.name) }}</div>
                </div>
                <div class="row">
                    <div class="offset-lg-3 col-lg-6 offset-md-1 col-md-10">{{ form_row(form.image) }}</div>
                </div>
                {{ form_widget(form) }}
                <div class="text-center">
                    <button class="btn btn-primary btn-lg">{{ button|default('Editer') }}</button>
                </div
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
